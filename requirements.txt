# Runtime dependencies for the Banciu chatbot app
anthropic>=0.25.0
rank-bm25>=0.2.2
streamlit>=1.35.0

# Preprocessing dependencies are intentionally split out into
# requirements-preprocess.txt so Streamlit/runtime installs stay lean.
